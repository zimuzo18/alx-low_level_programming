#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: the character to print
 *
 * Return : On succes 1.
 * On error, -1 is returned abd error is set apprioprately
 */
int _putchar(char c)
{
  return (write(1, &c, 1));
}
